
<link rel="stylesheet" type="text/css" href="/css/characters.css">

<div class="container">
  <div class="character-content">
    <div class="cs-how">
      <h2>How it works.</h2>
      <ol>
        <li>Select a character</li>
        <li>Add perks to your character's loadout.</li>
        <li>Save your build (create a free account to save your builds!)
        <li>Share your build for others to rate</li>
      </ol>
    </div>
    <div class="container-characters">
      <div class="fpbox" id="fpsurvivors" style="text-align: center;">
        <div class="heading"><a href="/Survivors" title="Survivors"><%= @character.name %></a>
        </div>
        <div class="fplinks">
              <div class="fplink plainlinks image">
                <div class="box">
                  <div class="row">
                    <div class="cell">
                      <div class="image">
                        <a href="/characters/<%= @character.slug%>" title="<%= @character.name %>"><img alt="<%= @character.name %> Preview Portrait.png" src="../images/characters/<%=@character.name%>.png" width="190" height="210" /></a>
                      </div>
                      <div class="link">
                        <a href="/characters/<%= @character.slug%>" title="<%= @character.name %>"><%= @character.name %></a>
                      </div>
                    </div>

                  </div>
                </div>
              </div>
          <div class="bio">
            <p><%= @character.bio %></p>
          </div>
        </div>
      </div>
    </div>

  </div>

</div>
