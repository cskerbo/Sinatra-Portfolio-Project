<link rel="stylesheet" type="text/css" href="/css/characters.css">

<div class="container">
<div class="character-content">
  <div class="cs-how">
  <h2>Create a custom character build</h2><br>
  <ol>
    <li>Select a character.</li><br>
    <li>Assign up to 4 perks to your character's loadout.</li><br>
    <li>Generate a random build using the perk randomizer!</li><br>
    <li>Create a free account to save as many custom builds as you want, share your build with other users, and to rate perks other builds!</li><br>
  </ol>
</div>
<div class="container-characters">
  <div class="fpbox" id="fpsurvivors" style="text-align: center;">
  <div class="heading"><a href="/Survivors" title="Survivors">The Survivors</a>
  </div>
  <div class="fplinks">
  <% @characters.each do |c|%>
  <% if c.character_type == "survivor"%>
  <div class="fplink plainlinks image">
  <div class="box">

  <div class="row">

  <div class="cell">

  <div class="image">

  <a href="/characters/<%= c.slug%>" title="<%= c.name %>"><img alt="<%= c.name %> Preview Portrait.png" src="images/characters/<%=c.name%>.png" width="190" height="210" /></a>
  </div>

  <div class="link">

  <a href="/characters/<%= c.slug%>" title="<%= c.name %>"><%= c.name %></a>
  </div>
  </div>
  </div>
  </div>
  </div>
  <%end%>
  <%end%>
  </div>

  </div>
</div>
<div class="container-characters">
  <div class="fpbox" id="fpkillers" style="text-align: center;">
  <div class="heading"><a href="/Killers" title="Killers">The Killers</a>
  </div>
  <div class="fplinks">
  <% @characters.each do |c|%>
  <% if c.character_type == "killer"%>
  <div class="fplink plainlinks image">
  <div class="box">
  <div class="row">
  <div class="cell">
  <div class="image">
  <a href="characters/<%= c.slug%>" title="<%= c.name %>"><img alt="<%= c.name %> Preview Portrait.png" src="images/characters/<%=c.name%>.png" width="190" height="210" /></a>
  </div>
  <div class="link">
  <a href="characters/<%= c.slug%>" title="<%= c.name %>"><%= c.name %></a>
  </div>
  </div>
  </div>
  </div>
  </div>
  <%end%>
  <%end%>
  </div>
  </div>
</div>
</div>

</div>
